import { RouteLoadingShell } from "@/components/route-loading-shell";

export default function ResourcesLoading() {
  return (
    <RouteLoadingShell
      title="자료실"
      description="법인, 카페, 바자울, 교육 자료를 불러오는 중입니다."
      variant="resources"
    />
  );
}
