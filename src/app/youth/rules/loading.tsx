import { RouteLoadingShell } from "@/components/route-loading-shell";

export default function YouthRulesLoading() {
  return (
    <RouteLoadingShell
      title="규칙"
      description="청소년 규칙 목록을 불러오는 중입니다."
      variant="resources"
    />
  );
}
